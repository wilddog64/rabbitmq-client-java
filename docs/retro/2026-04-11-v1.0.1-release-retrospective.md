# Retrospective — rabbitmq-client-java v1.0.1

**Date:** 2026-04-11
**Milestone:** v1.0.1 — NPE fix release
**PR:** #4 — merged to main (`295459c9`)
**Tag:** v1.0.1
**Participants:** Claude, Codex, Copilot

## What Went Well

- Copilot reviewed all 5 files with no findings — clean release PR
- `Build and Test` + `Publish to GitHub Packages` CI jobs passed on both PR and main
- Handoff to Codex (PR #3, the actual NPE fix) and to PR #4 (version bump/CHANGELOG) were clean two-step process
- `enforce_admins` restore cycle completed without friction
- GitHub Release created and JAR published to GitHub Packages on first attempt

## What Went Wrong

- `Integration Tests` job fails on every run due to HashiCorp apt key flake in `Configure Vault` step — pre-existing issue unrelated to this release; should be fixed as a separate task

## Decisions Made

- v1.0.1 scope: version bump + CHANGELOG only — no code changes; the actual NPE fix (PR #3) was separate
- Next branch: `docs/next-improvements` (already existed from prior work)
- Release notes drawn directly from CHANGELOG `[1.0.1]` section

## Theme

A clean maintenance release: the NPE fix code shipped in PR #3 (2026-04-10), PR #4 just
cut the release artifact. Two-PR pattern (fix first, bump second) keeps the commit graph
readable — the tag lands on the exact merge commit for the version bump, not mixed with
the fix code.
